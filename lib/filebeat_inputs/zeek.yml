# SOF-ELK® Configuration File
# (C)2026 Lewes Technology Consulting, LLC
#
# This file creates filebeat prospectors for Zeek source data from the SOF-ELK® VM itself

- type: filestream
  id: zeek-conn-01
  paths:
    - /logstash/zeek/**/conn.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_conn
  tags: [ 'zeek' ]

- type: filestream
  id: zeek-dns-01
  paths:
    - /logstash/zeek/**/dns.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
#TODO: we are ingesting zeek_dns type here but i dont think this is in the preprocess_all file (maybe others???)
         type: zeek_dns
  tags: [ 'zeek' ]

- type: filestream
  id: zeek-http-01
  paths:
    - /logstash/zeek/**/http.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_http
  tags: [ 'zeek' ]

- type: filestream
  id: zeek-files-01
  paths:
    - /logstash/zeek/**/files.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_files
  tags: [ 'zeek' ]

- type: filestream
  id: zeek-ssl-01
  paths:
    - /logstash/zeek/**/ssl.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_ssl
  tags: [ 'zeek' ]

- type: filestream
  id: zeek-x509-01
  paths:
    - /logstash/zeek/**/x509.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_x509
  tags: [ 'zeek' ]

- type: filestream
  id: zeek-ftp-01
  paths:
    - /logstash/zeek/**/ftp.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_ftp
  tags: [ 'zeek' ]

- type: filestream
  id: zeek-notice-01
  paths:
    - /logstash/zeek/**/notice.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_notice
  tags: [ 'zeek' ]

- type: filestream
  id: zeek-weird-01
  paths:
    - /logstash/zeek/**/weird.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_weird
  tags: [ 'zeek' ]

# using modbus_detailed.* because those contain response values
- type: filestream
  id: zeek-modbus-01
  paths:
    - /logstash/zeek/**/modbus_detailed.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_modbus_detailed
  tags: [ 'zeek' ]

# using omron_fins_detail.* because it contains a superset of fields from omron_fins_general
- type: filestream
  id: zeek-omron-fins-01
  paths:
    - /logstash/zeek/**/omron_fins_detail.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_omron_fins_detail
  tags: [ 'zeek' ]

- type: filestream
  id: zeek-enip-01
  paths:
    - /logstash/zeek/**/enip.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_enip
  tags: [ 'zeek' ]

- type: filestream
  id: zeek-cip-01
  paths:
    - /logstash/zeek/**/cip.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_cip
  tags: [ 'zeek' ]

### TODO: I dont think any of the following types are being handled at all
- type: filestream
  id: zeek-dhcp-01
  paths:
    - /logstash/zeek/**/dhcp.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_dhcp
  tags: [ 'zeek' ]

- type: filestream
  id: zeek-dpd-01
  paths:
    - /logstash/zeek/**/dpd.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_dpd
  tags: [ 'zeek' ]

- type: filestream
  id: zeek-irc-01
  paths:
    - /logstash/zeek/**/irc.*
  compression: auto
  prospector.scanner.exclude_files: [ '\.bz2$', '\.zip$' ]
  close.on_state_change.inactive: 5m
  clean_removed: true
  processors:
    - add_labels:
       labels:
         type: zeek_irc
  tags: [ 'zeek' ]
