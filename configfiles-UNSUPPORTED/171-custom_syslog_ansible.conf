# SOF-ELK Configuration File
# (C)2016 Lewes Technology Consulting, LLC
#
# This file contains filters, transforms, and enrichments for ansible system configuration messages
# Note that this file is UNSUPPORTED.  By default, it is not enabled in SOF-ELK.

# ansible-command:
#  Invoked with warn=True executable=None _uses_shell=True _raw_params=if [ $( rpm -q kernel | sort -nr | head -n 1 ) != kernel-$( uname -r ) ]; then echo 'reboot'; else echo 'no'; fi removes=None creates=None chdir=None
# ansible-yum:
#  Invoked with name=['*'] list=None disable_gpg_check=False conf_file=None install_repoquery=True state=latest disablerepo=None update_cache=True enablerepo=None exclude=None
# ansible-setup:
#  Invoked with filter=* fact_path=/etc/ansible/facts.d
# ansible-service:
#  Invoked with name=rsyslog pattern=None enabled=True state=started sleep=None arguments= runlevel=default
# ansible-file:
#  Invoked with directory_mode=None force=False remote_src=None path=/etc/ owner=root follow=False group=root state=None content=NOT_LOGGING_PARAMETER serole=None diff_peek=None setype=None dest=/etc/ selevel=None original_basename=rsyslog.conf regexp=None validate=None src=rsyslog.conf seuser=None recurse=False delimiter=None mode=0644 backup=None
# ansible-stat:
#  Invoked with checksum_algorithm=sha1 mime=False get_checksum=True path=/etc/rsyslog.conf checksum_algo=sha1 follow=False get_md5=False
# ansible-ping:
#  Invoked with data=None
