---
# Ansible file supporting the build of a SOF-ELK(R) standalone system
# (C)2026 Lewes Technology Consulting, LLC

dependencies:
  - role: debian-apt-source
    when: ansible_facts['distribution'] == "Debian" and ansible_facts['distribution_major_version'] is version('12', '>=')
