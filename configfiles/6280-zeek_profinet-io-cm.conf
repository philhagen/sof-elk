# SOF-ELK® Configuration File
# (C)2025 Lewes Technology Consulting, LLC
#
# This file contains processing steps for Zeek's profinet_io_cm.log, created by https://github.com/cisagov/ICSNPP
# See https://github.com/cisagov/icsnpp-profinet-io-cm

filter {
  if [labels][type] == "zeek_profinet_io_cm" and "zeek_json" in [tags] {
    mutate {
      rename => {
        "[raw][uid]" => "[zeek][session_id]"
        "[raw][id.orig_h]" => "[source][ip]"
        "[raw][id.orig_p]" => "[source][port]"
        "[raw][id.resp_h]" => "[destination][ip]"
        "[raw][id.resp_p]" => "[destination][port]"
        "[raw][xxx]" => "[profinet][xxx]"
        "[raw][xxx]" => "[profinet][xxx]"
        "[raw][xxx]" => "[profinet][xxx]"
        "[raw][xxx]" => "[profinet][xxx]"
        "[raw][xxx]" => "[profinet][xxx]"
        "[raw][xxx]" => "[profinet][xxx]"
        "[raw][xxx]" => "[profinet][xxx]"
        "[raw][xxx]" => "[profinet][xxx]"
      }
    }

    # populate the @timestamp field with the ts value
    date {
      match => [ "[raw][ts]", "UNIX" ]
    }

    mutate {
      remove_field => "raw"
    }
  }
}
