output {
  if [type] == "l2tcsv" {
    elasticsearch {
      index => "log2timeline-%{YYYY.MM.dd}"
      template => "/usr/local/sof-elk/lib/elasticsearch-log2timeline-template.json"
      template_name => "log2timeline"
      template_overwrite => true
    }
  }
}